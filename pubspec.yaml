name: dev_yahia
description: Portfolio for Yahia Salem (@DevYahia)

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 4.0.0+4

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # icons
  community_material_icon: ^5.9.55
  cupertino_icons: ^1.0.4
  # links
  url_launcher: ^6.1.8
  url_launcher_web: ^2.0.11
  mailto: ^2.0.0
  # image
  image_picker: ^0.8.6+1
  # misc
  fluttertoast: ^8.1.2
  url_strategy: ^0.2.0
  universal_html: ^2.0.8
  # firebase
  firebase_core_web: ^2.1.0
  firebase_core: ^2.4.1
  cloud_firestore: ^4.3.1
  firebase_storage: ^11.0.10
  # ui
  google_fonts: ^3.0.1
  animated_text_kit: ^4.2.2
  scrollable_positioned_list: ^0.3.5
  auto_animated: ^3.1.0
  # state
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
